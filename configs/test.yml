server:
  address: ":8080"
  timeout: 50
database:
  url: root:@tcp(localhost:3308)/accounts?parseTime=true
  maxIdleConns: 5
  maxOpenConns: 50
  connMaxLifetime: 60
  autoMigrate: true
logger:
  level: "debug"